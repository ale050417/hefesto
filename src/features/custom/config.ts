/**
 * Flag del feature "Pedidos a medida". En false, las pantallas (cliente y
 * admin) muestran la pantalla "en construcción" sin borrar el código: el
 * feature sigue completo y se reactiva poniendo esto en true.
 *
 * TODO: volver a true cuando el flujo a medida esté listo para producción.
 */
export const CUSTOM_ORDERS_ENABLED: boolean = true;
